<!-- pages/classInfo/classInfo.wxml -->
<view class="box" wx:if="{{classInfo.length !== 0}}">
  <view class="item" wx:for="{{classInfo}}" wx:for-item="item">
    <view class="item_left">{{item.name}}</view>
    <view class="item_right">
      <van-icon name="arrow" />
    </view>
  </view>
</view>
<view class="box2" wx:if="{{classInfo.length == 0}}">
  <view>此课程暂无资料~~~</view>
</view>